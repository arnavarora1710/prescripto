spring.application.name=healthai

# Logging configuration
logging.level.root=INFO
logging.level.org.springframework=INFO
logging.level.com.hacktech.healthai=DEBUG

# Server configuration
server.port=8080

# supabase
spring.datasource.url=jdbc:postgresql://${SUPABASE_HOST}:6543/postgres?sslmode=require
spring.datasource.username=${SUPABASE_DB_USER}
spring.datasource.password=${SUPABASE_DB_PWD}

# Spring Datasource Configuration (Supabase via Pooler)
# spring.datasource.url=jdbc:postgresql://aws-0-us-west-1.pooler.supabase.com:6543/postgres?sslmode=require
# spring.datasource.username=postgres.lgnpqwgshjtnjyyxusqa
# spring.datasource.password=${SUPABASE_DB_PASSWORD}
# spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
# Be careful with ddl-auto in production! 'validate' or 'none' might be safer.
# spring.jpa.hibernate.ddl-auto=update
# Log SQL statements (useful for debugging)
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# Format logged SQL
# spring.jpa.properties.hibernate.format_sql=true

# Supabase configuration (if needed directly, though unlikely with JPA/JDBC setup)
# supabase.url=${SUPABASE_URL}
# supabase.key=${SUPABASE_ANON_KEY}
